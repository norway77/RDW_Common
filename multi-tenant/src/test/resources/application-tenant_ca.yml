#simulating the spring cloud config search path feature with "tenant_nv" profile for testing purposes
spring:
  datasource:
    tenants:
      tenant_ca:
        url-parts:
          database: "tenant_ca"
        username: "tenant_ca_user"
        password: "tenant_ca_password"
        validationQueryTimeout: 50

tenant:
    id: ca-0000000000000000000
    name: "California"
    aws:
      bucket: s3://new-bucket
      object-prefix: "tenant_ca"